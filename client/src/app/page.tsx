// client/src/app/page.tsx
export default function HomePage() {
  // Middleware handles redirects based on role
  return (
    <main className="flex items-center justify-center h-screen">
      <p>Redirecting...</p>
    </main>
  );
}
